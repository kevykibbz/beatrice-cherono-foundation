import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        softPurple: '#F3E8FF', 
      },
    },
  },
  plugins: [],
};

export default config;
